/* Shammah Thao 
 * Compute for lab 5 */ 
/*----------------------------------------------------------*/
#include "lab5.h"
#include <stdio.h>
#include <math.h>

void compute(double radius, double *area, double *cir){

*area =( (M_PI) * (radius * radius));
*cir =((2) * (M_PI) * (radius));
return;
}
/*------------------------------------------------------------*/
